apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: myapp
  namespace: default
spec:
  host: myapp
  subsets:
  - name: canary
    labels:
      app: v1
  - name: stable
    labels:
      app: v2
